# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Order {
  id: Int!
  orderNumber: String!
  total: Float!
  status: OrderStatus!
  createdAt: DateTime!
  updatedAt: DateTime!
  userId: Int!
  carrierId: Int
}

enum OrderStatus {
  PENDING
  PAID
  CANCELLED
  REFUNDED
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Carrier {
  id: Int!
  name: String!
  apiKey: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type ShippingRate {
  id: Int!
  carrierId: Int!
  serviceName: String!
  rate: Float!
  estimatedDeliveryDays: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Shipment {
  id: Int!
  trackingNumber: String!
  status: ShipmentStatus!
  orderId: Int!
  carrierId: Int!
  shippedAt: DateTime
  deliveredAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum ShipmentStatus {
  PENDING
  SHIPPED
  IN_TRANSIT
  DELIVERED
  CANCELLED
}

type Return {
  id: Int!
  returnNumber: String!
  status: ReturnStatus!
  reason: String!
  pickupScheduledAt: DateTime
  orderId: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum ReturnStatus {
  REQUESTED
  APPROVED
  REJECTED
  COMPLETED
  CANCELLED
}

type PluginInfo {
  name: String!
  version: String!
  author: String!
  description: String
  requiredPermissions: [String!]
  dependencies: [String!]
  uiComponents: [PluginUIComponent!]
  enabled: Boolean!
}

type PluginUIComponent {
  id: String!
  name: String!
  description: String
  route: String!
}

type PluginDependencyStatusType {
  name: String!
  dependenciesMet: Boolean!
  missingDependencies: [String!]!
}

type Role {
  id: Int!
  name: String!
  description: String
}

type Query {
  sayHello: String!

  """Get all orders ordered by creation date (newest first)"""
  orders: [Order!]!

  """Get a specific order by ID"""
  order(
    """The ID of the order"""
    id: Int!
  ): Order!

  """Filter orders based on status, user ID, carrier ID, or order number"""
  filterOrders(
    """Filter criteria for orders"""
    ordersFilterInput: OrdersFilterInput!
  ): [Order!]!

  """Get orders by user ID"""
  ordersByUser(
    """The user ID to filter by"""
    userId: Int!
  ): [Order!]!

  """Get orders by status"""
  ordersByStatus(
    """The status to filter by"""
    status: OrderStatus!
  ): [Order!]!

  """Get counts of orders by status"""
  orderCountsByStatus: String!

  """Get total sales amount from paid orders"""
  totalSales: Float!

  """Get all shipping carriers"""
  carriers: [Carrier!]!

  """Get a carrier by ID"""
  carrier(
    """The ID of the carrier"""
    id: Int!
  ): Carrier!

  """Get all shipping rates ordered by carrier and rate"""
  shippingRates: [ShippingRate!]!

  """Get a shipping rate by ID"""
  shippingRate(
    """The ID of the shipping rate"""
    id: Int!
  ): ShippingRate!

  """Get all shipping rates for a specific carrier"""
  shippingRatesByCarrier(
    """The ID of the carrier"""
    carrierId: Int!
  ): [ShippingRate!]!

  """Get the cheapest shipping rate"""
  cheapestShippingRate: ShippingRate

  """Get the fastest shipping rate"""
  fastestShippingRate: ShippingRate

  """Get the best value shipping rate (balancing cost and speed)"""
  bestValueShippingRate: ShippingRate

  """Get all shipments ordered by creation date (newest first)"""
  shipments: [Shipment!]!

  """Get a specific shipment by ID"""
  shipment(
    """The ID of the shipment"""
    id: Int!
  ): Shipment!

  """Filter shipments based on status, order ID, or carrier ID"""
  filterShipments(
    """Filter criteria for shipments"""
    shipmentsFilterInput: ShipmentsFilterInput!
  ): [Shipment!]!

  """Get shipments by status"""
  shipmentsByStatus(
    """The status to filter by"""
    status: ShipmentStatus!
  ): [Shipment!]!

  """Get shipments by order ID"""
  shipmentsByOrder(
    """The order ID to filter by"""
    orderId: Int!
  ): [Shipment!]!

  """Get counts of shipments by status"""
  shipmentCountsByStatus: String!

  """Get all returns ordered by creation date (newest first)"""
  returns: [Return!]!

  """Get a specific return by ID"""
  return(
    """The ID of the return"""
    id: Int!
  ): Return!

  """Filter returns based on status or order ID"""
  filterReturns(
    """Filter criteria for returns"""
    returnsFilterInput: ReturnsFilterInput!
  ): [Return!]!

  """Get returns by status"""
  returnsByStatus(
    """The status to filter by"""
    status: ReturnStatus!
  ): [Return!]!

  """Get returns by order ID"""
  returnsByOrder(
    """The order ID to filter by"""
    orderId: Int!
  ): [Return!]!

  """Get counts of returns by status"""
  returnCountsByStatus: String!

  """List all loaded plugins"""
  plugins: [PluginInfo!]!

  """List all enabled plugins"""
  enabledPlugins: [PluginInfo!]!

  """List all UI plugin components from enabled plugins"""
  uiPluginComponents: [PluginUIComponent!]!

  """Get dependency status for a plugin"""
  pluginDependencyStatus(name: String!): PluginDependencyStatusType!
  roles: [Role!]!
  role(id: Int!): Role!
}

input OrdersFilterInput {
  status: OrderStatus
  userId: Int
  carrierId: Int
  orderNumber: String
}

input ShipmentsFilterInput {
  status: ShipmentStatus
  orderId: Int
  carrierId: Int
}

input ReturnsFilterInput {
  status: ReturnStatus
  orderId: Int
}

type Mutation {
  """Create a new order"""
  createOrder(
    """Order creation data"""
    createOrderInput: CreateOrderInput!
  ): Order!

  """Update an existing order"""
  updateOrder(
    """Order update data"""
    updateOrderInput: UpdateOrderInput!
  ): Order!

  """
  Delete an order (only allowed for orders without shipments or returns and not in PAID status)
  """
  deleteOrder(
    """The ID of the order to delete"""
    id: Int!
  ): Order!

  """Create a new shipping carrier"""
  createCarrier(
    """Carrier creation data"""
    createCarrierInput: CreateCarrierInput!
  ): Carrier!

  """Update an existing shipping carrier"""
  updateCarrier(
    """Carrier update data"""
    updateCarrierInput: UpdateCarrierInput!
  ): Carrier!

  """Delete a shipping carrier"""
  deleteCarrier(
    """The ID of the carrier to delete"""
    id: Int!
  ): Carrier!

  """Create a new shipping rate"""
  createShippingRate(
    """Shipping rate creation data"""
    createShippingRateInput: CreateShippingRateInput!
  ): ShippingRate!

  """Update an existing shipping rate"""
  updateShippingRate(
    """Shipping rate update data"""
    updateShippingRateInput: UpdateShippingRateInput!
  ): ShippingRate!

  """Delete a shipping rate"""
  deleteShippingRate(
    """The ID of the shipping rate to delete"""
    id: Int!
  ): ShippingRate!

  """Create a new shipment"""
  createShipment(
    """Shipment creation data"""
    createShipmentInput: CreateShipmentInput!
  ): Shipment!

  """Update an existing shipment"""
  updateShipment(
    """Shipment update data"""
    updateShipmentInput: UpdateShipmentInput!
  ): Shipment!

  """Delete a shipment (only allowed for non-delivered shipments)"""
  deleteShipment(
    """The ID of the shipment to delete"""
    id: Int!
  ): Shipment!

  """Create a new return request"""
  createReturn(
    """Return creation data"""
    createReturnInput: CreateReturnInput!
  ): Return!

  """Update an existing return"""
  updateReturn(
    """Return update data"""
    updateReturnInput: UpdateReturnInput!
  ): Return!

  """
  Delete a return (only allowed for returns with status REQUESTED or REJECTED)
  """
  deleteReturn(
    """The ID of the return to delete"""
    id: Int!
  ): Return!

  """Enable a plugin by name"""
  enablePlugin(name: String!): Boolean!

  """Disable a plugin by name"""
  disablePlugin(name: String!): Boolean!
  createRole(createRoleInput: CreateRoleInput!): Role!
  updateRole(updateRoleInput: UpdateRoleInput!): Role!
  removeRole(id: Int!): Role!
}

input CreateOrderInput {
  orderNumber: String!
  total: Float!
  userId: Int!
  status: OrderStatus
  carrierId: Int
}

input UpdateOrderInput {
  id: Int!
  orderNumber: String
  total: Float
  status: OrderStatus
  carrierId: Int
}

input CreateCarrierInput {
  name: String!
  apiKey: String!
}

input UpdateCarrierInput {
  id: Int!
  name: String
  apiKey: String
}

input CreateShippingRateInput {
  carrierId: Int!
  serviceName: String!
  rate: Float!
  estimatedDeliveryDays: Int!
}

input UpdateShippingRateInput {
  id: Int!
  carrierId: Int
  serviceName: String
  rate: Float
  estimatedDeliveryDays: Int
}

input CreateShipmentInput {
  trackingNumber: String!
  status: ShipmentStatus!
  orderId: Int!
  carrierId: Int!
  shippedAt: DateTime
  deliveredAt: DateTime
}

input UpdateShipmentInput {
  id: Int!
  trackingNumber: String
  status: ShipmentStatus
  shippedAt: DateTime
  deliveredAt: DateTime
}

input CreateReturnInput {
  returnNumber: String!
  status: ReturnStatus!
  reason: String!
  pickupScheduledAt: DateTime
  orderId: Int!
}

input UpdateReturnInput {
  id: Int!
  returnNumber: String
  status: ReturnStatus
  reason: String
  pickupScheduledAt: DateTime
}

input CreateRoleInput {
  name: String!
  description: String
}

input UpdateRoleInput {
  id: Int!
  name: String
  description: String
}