# Rate Shop Engine

Now includes surcharges and pincode serviceability:
- `RateSurcharge`: percent and flat per carrier
- `PincodeZone`: zone, ODA flag, per-carrier overrides (future)
- Idempotent tracking events via `externalId` on `TrackingEvent`
